// STUB (stage 16): the only file in the TreeSupport SRC set that includes CutUtils.hpp is
// TriangleMesh.cpp, and it references NO Cut symbol (the include is vestigial). The real header
// pulls the full Model.hpp graph (SLA/SupportPoint.hpp, CustomGCode, EmbossShape, ...) which the
// kernel has no use for. Empty stub breaks that chain; documented, no behavior change.
#ifndef slic3r_CutUtils_hpp_
#define slic3r_CutUtils_hpp_
#endif
